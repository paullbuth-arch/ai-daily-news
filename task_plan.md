# 任务计划：嵌入式知识库 2.0 全库深度重构

## 目标

在前期评估和规划基础上，重构知识库的全部有效学习/项目文档，使其能够系统提升嵌入式工程师的技术能力。每篇技术文档都要以费曼技巧解释概念、即时解析术语，并锚定到 `~/wq7036a` 或 `~/aiglass` 中的真实 SDK/BSP 代码；后续由接手的 AI 直接改写，我负责最终质量验收。

## 阶段

- [completed] 阶段 1：通读目标文档，提炼能力模型与验收标准
- [completed] 阶段 2：盘点知识库目录、文档规模、主题覆盖和内容形态
- [completed] 阶段 3：将知识库现状映射到目标能力模型，识别差距
- [completed] 阶段 4：设计深度与广度扩展路线、优先级和验收指标
- [completed] 阶段 5：整理结论与后续执行计划
- [completed] 阶段 6：建立全库改写规范、范围清单与双 SDK 溯源映射
- [paused] 阶段 7：按主题批次直接改写并记录实际代码锚点（交接给后续 AI）
- [pending] 阶段 8：逐批验收技术准确性、SDK 对应关系、链接和 Markdown 质量
- [pending] 阶段 9：全库一致性复核、缺口补写与交付总结

## 规划原则

- 广度优先补“工程生命周期”和“AI 协作治理”这两条横向能力，不继续无边界增加零散协议主题。
- 深度优先选少数高风险、跨层、可验证的垂直案例，把概念、真实硬件、代码、测试、发布、回滚和复盘串起来。
- 每条事实区分“概念说明 / 项目假设 / 已验证事实 / 生产证据”，避免把示例代码和实际结论混在一起。
- 以 reGlasses/WQ7036AX + V881 作为现有主线，同时为目标文档涉及的 reComputer/XIAO/SG2002 等平台建立可替换的平台上下文层。
- 不再将“目录覆盖”视作“知识深度”：每个高频技术点必须覆盖原理、约束、真实代码、异常路径、调试与练习。
- 代码示例优先抽取、解释和定位真实 SDK/BSP 代码；无直接实现时明确说明缺口，不伪造项目 API。
- 用小批次重写：一个主题目录或一个项目子域为一个委托批次，模型只能改该批次文件；每批先验收再继续。

## 全库改写规范

### 用户明确要求

1. 用费曼技巧把概念解释清楚。
2. 新名词和相关术语在首次出现处给出解释。
3. 结合真实 SDK 讲解，不使用脱离项目的通用代码替代实现。
4. 代码来源为 `~/wq7036a` 与 `~/aiglass`。

### 补充质量要求

1. 技术文档有明确的学习目标、前置知识、核心结论、边界/反例、调试方法、练习与自测。
2. 每个 SDK 片段都要注明相对源路径、函数/结构体/配置项名、适用平台和它在系统中的作用；长代码只摘取关键片段并解释调用链。
3. 通用协议事实与项目事实必须分开写；项目事实无法从实际源码或配置确认时标注“待以当前 SDK/原理图确认”。
4. Obsidian 链接必须指向已有页面；每篇技术文档至少链接到上游基础、同层相关和项目应用中的一项。
5. 已弃用的 STTP 归档只维护“为何弃用、替代方案、迁移入口”，不扩展为新教程。

### 源码锚点

- WQ7036AX：`/home/ys/wq7036a/wq-audio/`；应用层在 `wq-adk/`，核心平台在 `wqcore/`，包括 driver、FreeRTOS、蓝牙、音频、IPC、OTA、启动与构建系统。
- V861/reGlasses：`/home/ys/aiglass/reglasses/` 是唯一可编辑 BSP 来源；`/home/ys/aiglass/tina-v861/` 仅用于只读参考，禁止模型或本任务直接编辑。

### 范围假设

- 改写范围：`00-Inbox`、根目录学习路线、`10` 至 `95` 下所有有效学习与项目 Markdown。
- 不改写：《AI时代的软件手艺人.md》、本任务的 `task_plan.md`/`findings.md`/`progress.md` 与 `.obsidian` 配置。
- `_archive-STTP` 文件仅做弃用/迁移信息和链接健康整理，保持历史资料的可追溯性。

## 关键决策

- 先以目标文档提出的“软件手艺人/AI 时代工作方式”能力要求为评价基准，再评价知识库，而不是以文件数量代替能力覆盖。
- 同时检查知识内容本身和知识库的导航、证据、项目实战、复盘闭环；后者会直接影响“能否满足要求”。

## 错误与阻塞

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 交互式 Claude Code 会话无法通过当前执行通道输入 | 1 | 已终止；改用 CLI 的 `-p` 非交互模式。 |
| 非交互调用的多行提示词被 Shell 转义截断 | 1 | 模型未修改文件；下一次改为让模型读取受控任务文件或使用无歧义参数传递。 |
| 在编排环境中使用 `TextEncoder` 编码提示词失败 | 1 | 改用受控任务文件作为提示词载体，不再依赖该运行时 API。 |
