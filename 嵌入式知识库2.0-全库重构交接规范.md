---
type: meta
created: 2026-07-17
tags: [knowledge-base, handoff, embedded, feynman, sdk, quality]
aliases: [知识库重构交接规范, 全库改写标准]
---

# 嵌入式知识库 2.0：全库重构交接规范

> 这是一份执行文档，不是技术教程。接手的 AI 必须先读本文件，再读 `task_plan.md`、`findings.md`、`progress.md` 和《AI时代的软件手艺人.md》，然后再修改知识库。

## 1. 任务目标

把当前知识库从“覆盖面较广的高密度提纲”重构成能让**零基础或基础薄弱的学习者**逐步建立工程能力的学习系统。

本知识库的首要读者是小白：默认读者不熟悉英文缩写、芯片外设、时序图、C 代码、SDK 分层和调试工具。文档必须先让读者理解“它在解决什么生活化/工程化问题”，再逐层学习原理、真实 SDK 和异常处理；不能用术语密度或代码长度筛选读者。

目标不是让文件变多，也不是把每个主题压缩成几百行术语，而是让学习者能够完成一条可验证的能力链：

```text
理解原理 → 读懂约束 → 看懂真实 SDK → 写出/修改真实代码
       → 处理异常 → 使用工具定位 → 做实验验证 → 复盘并交付
```

这条能力链必须**分阶段交付**：小白第一遍只需完成“知道是什么、为什么需要、能描述正常过程”；第二遍才进入约束和最小 SDK 调用；第三遍才要求读调用链、处理异常和做工程取舍。深度内容不能删除，但不得抢占第一遍的阅读路径。

每个核心知识点至少要能回答：

1. 它解决什么问题，为什么这样设计？
2. 它在电气、协议、软件 API 和系统架构上有什么约束？
3. 当前两个真实 SDK 中对应的代码、配置、调用链在哪里？
4. 正常路径和异常路径分别是什么？
5. 出问题时看什么现象、提出什么假设、如何验证？
6. 学习者如何通过练习证明自己真的会，而不是看懂了文字？

## 2. 不可改变的用户要求

用户明确要求所有有效学习文档都满足：

- 使用费曼技巧，把概念讲到初学者能复述、工程师能应用；
- 新名词或相关术语在第一次出现时就解释，不能只堆英文缩写；
- 结合真实 SDK/BSP/项目代码讲解，不能用通用伪代码冒充项目实现；
- 真实代码来源只允许：`/home/ys/wq7036a` 和 `/home/ys/aiglass`；
- 用户已明确停止使用 DeepSeek，后续不要调用 DeepSeek 或 `/home/ys/wq7036a/model.sh`；接手 AI 直接完成改写，主 AI 负责验收。

## 3. 当前状态

### 已完成

`20-HardwareProtocol-硬件协议/UART-I2C-SPI-GPIO-串口总线/i2c-basics-I2C基础.md` 已经作为第一篇深度样板重写，约 720 行，已通过 `git diff --check`。

I2C 样板包含：

- 费曼式心智模型和协议逐拍解释；
- 开漏、上拉、电容、上升时间和电平转换；
- 7-bit 地址、线上地址字节、Linux 地址表达的区别；
- ACK/NACK、Repeated START、时钟拉伸、总线恢复；
- WQ HAL 的轮询、中断、组合写读和缓冲区边界；
- V861 Linux `i2c_adapter`、`i2c_msg`、`i2c_transfer`；
- TCA9539、设备树、摄像头 reset/pwdn 场景；
- NACK、busy、超时、SDA 拉低、读回异常的诊断路径；
- 练习、自测题、反例和验收标准。

### 尚未完成

硬件协议目录中除 I2C 外，其余文档仍大多是旧版提纲，尤其存在以下问题：

- SPI 仍包含不存在或未核验的通用 API 示例；
- UART 需要从 WQ HAL 真实接口重新建立配置、FIFO、中断、DMA、共享 IO 和应用调用链；
- GPIO 需要讲清资源占用、输入/输出时序、中断、上下拉、低功耗恢复和竞态；
- 音频接口、PWM、ADC/DAC、MIPI/USB/SDIO 等还需要按同一标准深化；
- MOC、上游/下游链接和术语命名需要全库复核；
- 其他目录的学习/项目 Markdown 仍需逐篇重构或明确标注为导航、索引、归档。

## 4. 重构范围

### 需要重构

- `00-Inbox/`
- 根目录有效学习路线和方法文档；
- `10` 至 `95` 下所有有效学习、项目、调试、测试、工具和 AI 协作 Markdown；
- 每个主题目录的 MOC、索引和入口页。

### 特殊处理

- 《AI时代的软件手艺人.md》是目标标准文档，只读，不要改写；
- `task_plan.md`、`findings.md`、`progress.md` 是任务记录，可更新但不要把它们改成教程；
- `_archive-STTP/` 只维护弃用原因、替代路线、迁移入口和链接健康，不扩写历史协议教程；
- `/home/ys/aiglass/tina-v861/` 是厂商 SDK，只读参考，禁止修改；
- `/home/ys/aiglass/reglasses/` 是可编辑的真实项目 BSP 来源，但本任务只读它，不要修改项目源码；
- 不要修改 `.obsidian/` 配置，尤其是用户已有的 workspace 修改。

## 5. 每篇技术文档的固定结构

不要求每篇文字完全相同，但核心技术文档必须尽量按下面结构组织：

```markdown
---
type: concept
tags: [...]
aliases: [...]
---

# 主题：从问题到真实 SDK

> 一句话结论：用一句可验证的话说清主题。

## 30 秒先看懂
用不超过 5 句话说明：它解决什么问题、生活化类比是什么、正常情况下会发生什么、初学者首先要记住什么。
本节不放大段代码、寄存器名、公式或未解释的英文缩写。

## 学完以后应该能做什么
分为“第一遍能做到”和“进阶后能做到”。第一遍只要求能描述、识别和完成一个最小操作；进阶才要求读 SDK、定位异常和迁移。

## 前置知识
链接到已有文档，并说明缺哪项基础。

## 术语先讲清楚
首次出现的缩写、英文、协议角色、硬件概念、SDK 类型/API 都要解释。每个术语先用一句白话解释，再给准确术语；不要在一个段落连续引入超过 3 个未学过的概念。

## 第一层：费曼式心智模型
先用日常类比，再明确类比的边界；解释它为什么存在。必须包含一个不依赖代码的最小场景，让读者能按步骤复述“开始前是什么状态、发生了什么、最后得到了什么”。

## 第二层：原理、时序和约束
给出数据流/状态机/时序/公式/边界条件；说明哪些是通用规范。本层是进阶内容，开头先说明“现在为什么需要理解这一层”，公式必须先解释变量的物理意义，再给计算。

## 第三层：真实 SDK 代码
先给一个最小的真实 SDK 片段，逐行解释输入、输出和它在前面场景中的位置；再给完整调用链。
注明绝对来源路径、相对仓库路径、函数/结构体/配置项、平台、调用者和作用。关键代码只能来自真实 SDK；长代码摘关键片段并解释上下文。

## 第四层：正常路径与异常路径
说明成功条件、返回值、超时、重试、资源释放、并发/缓存/生命周期问题。

## 第五层：调试方法
按“现象 → 假设 → 证据 → 结论 → 修复”写；尽量给出日志、波形、寄存器或测试结果应观察什么。

## 第六层：实战练习
至少 3 个，至少 1 个要求读真实源码，至少 1 个要求设计故障定位。

## 自测与验收
至少 5 个问题；答案要能区分背诵和真正理解。

## 延伸阅读
只链接实际存在的页面。
```

### 5.0 小白优先的教学规则

小白优先不等于删掉工程细节，而是控制首次接触时的认知负担：

- **先问题，后名词；先现象，后机制；先最小成功，后异常；先关键片段，后完整调用链。**
- 每个小节只回答一个核心问题。读者刚理解“为什么需要时钟”时，不要同时塞入 DMA、缓存、寄存器和低功耗恢复。
- 图、表、时序图和代码必须服务于一个明确问题；不能因为资料齐全而整块粘贴源码或枚举表。
- 代码块前先说明“它在场景的哪一步、输入是什么、成功后能看到什么”；代码块后用白话总结，不要求小白自行从 C 语法推导硬件行为。
- 真实 SDK 的复杂性必须显式分层：`最小调用`、`关键参数`、`完整调用链/进阶`。小白读完最小调用部分也应得到完整的正常路径认知。
- 前置知识不足时给出明确跳转和一句补课目标；不得用“读者自行学习”跳过关键概念。
- 任何一层结束都要有一个 1～3 分钟能完成的自检，例如指出信号方向、预测一次调用结果、用自己的话复述数据流；复杂源码追踪和故障注入放进进阶练习。

### 5.1 费曼技巧的合格标准

不能只写一个比喻。合格的费曼解释必须包含：

- “它是什么”：用自己的话定义；
- “为什么”：说明它解决的具体问题；
- “怎么工作”：按时序、数据流或状态变化讲清楚；
- “什么时候不成立”：指出边界、反例和常见误解；
- “如何验证”：给出一个可以观察到的证据。

对于协议、驱动和硬件主题，还必须有一个**完整场景演算**：从初始电平/状态、触发事件、状态变化、数据缓冲区内容、SDK 调用，到结束条件逐步说明。演算中的每个时间点、字节下标、缓冲区长度、返回值和电气结论必须自洽，并且与当前 SDK 的 API 契约一致。

不能为了让故事好懂而篡改实现语义。例如：一个 API 请求 3 字节接收，就不能在例子中访问第 4 个接收字节；驱动在命令阶段丢弃 MISO 数据时，必须区分“总线上的物理事实”和“调用者能从 API 读到的数据”。

例如，写 I2C 的开漏时，必须同时解释“设备如何产生低/高电平”“为什么允许多设备共享”“上拉和电容怎样影响上升时间”“内部上拉不等于板级设计完成”。只写“像多人拉绳子”不算讲透。

对小白而言，费曼解释的通过标准是：在不看术语表和代码的情况下，能回答“它解决什么问题”“正常时谁先做什么、谁后做什么”“如果少了其中一个条件会怎样”。如果答不出来，说明解释还停留在术语翻译，而不是建立心智模型。

### 5.1.1 场景演算的准确性门槛

场景演算是教学重点，也是最容易引入错误的地方。每一个演算都必须通过下面检查：

- 时间轴必须符合协议时序。比如 UART 应区分起始位确认与第一个数据位采样，不能把两者混为同一个时刻；误差预算必须说明是收发双方的总误差还是单侧误差。
- 缓冲区下标、长度和所有权必须符合真实 API。不得访问未请求/未分配的字节，不得把硬件内部丢弃的阶段误写成用户缓冲区内容。
- 电气结论必须符合驱动结构。推挽输出主动拉低时，普通上拉电阻不能把线维持为高；“上拉保证默认安全”只适用于引脚高阻、复位或开漏释放等对应条件。
- 并发结论必须以源码或硬件原子性为证据。不能把两个操作组成的 `toggle`、读改写或回调链称为原子；应说明单一所有者、临界区、锁或硬件原子寄存器等实际保障。
- 频率、FIFO 深度、上拉阻值、驱动强度、采样算法等数字必须能追溯到当前 SDK、芯片手册或实测证据；找不到证据时写“待数据手册/实测确认”，不要填入看似合理的数字。

### 5.2 术语解析规则

- 第一次出现写成：`中文名（English，全称/缩写）：在本主题中的具体含义`；
- 不能假定读者知道 `FIFO`、`DMA`、`ISR`、`HAL`、`BSP`、`CS`、`CPOL`、`CPHA`、`device tree` 等术语；
- 同一篇文档后续可以使用缩写，但不要在首次出现前先使用缩写；
- SDK 私有名词必须说明“这是项目 API/类型，不是协议标准”；
- 名词解释必须服务于当前问题，不能复制一大段百科。

## 6. 真实 SDK 溯源标准

### 6.1 WQ7036AX（`/home/ys/wq7036a`）

主要代码根：

- HAL：`wq-audio/wqcore/driver/periph/common/hal/`
- 硬件层：`wq-audio/wqcore/driver/periph/bbb/hw/`
- 应用示例：`wq-audio/wq-adk/examples/glass/acore/app/src/`
- 应用扩展：`wq-audio/wq-adk/examples/glass/acore/app/app_customer_ext_trans/`

引用 WQ 代码时必须写：

1. 文件相对路径；
2. 符号名（函数、枚举、结构体、宏或配置项）；
3. 它被谁调用或它调用了谁；
4. 这段代码证明了什么，不能证明什么；
5. 若有配置开关，说明适用条件。

### 6.2 V861/reGlasses（`/home/ys/aiglass`）

主要代码根：

- 可编辑项目：`reglasses/`
- VIN/摄像头驱动：`reglasses/bsp/drivers/vin/modules/`
- 设备树：`reglasses/device/configs/reglasses/linux-6.6-xuantie/board.dts`
- Linux 内核/驱动集成：以 `reglasses` 当前源码为准；`tina-v861/` 只读。

引用 Linux/V861 代码时必须区分：

- Linux 通用框架事实，如 `i2c_msg` 的语义；
- reGlasses 项目事实，如具体设备地址、GPIO、时钟、设备树节点；
- 尚未由当前源码或板级资料证实的推断，必须标为“待确认”。

### 6.3 禁止事项

- 禁止写 `spi_transfer(...)`、`uart_init(...)` 等未在当前 SDK 中确认存在的伪 API；
- 禁止把 Arduino、ESP-IDF、STM32 HAL、Linux 用户态示例当成 WQ SDK 代码；
- 可以用协议级伪代码说明时序，但必须明确标为“通用伪代码”，并紧接着给出真实 SDK 对应关系或说明缺口；
- 不得凭印象填写芯片地址、GPIO 号、时钟频率、DMA 通道或设备树属性；
- 禁止把通用示意代码伪装成 WQ/reGlasses SDK 代码。诸如 `os_signal()`、`delay_ms()`、`on_event()` 一类未在当前工程中定位到的调用，只能明确标为“协议级伪代码”；优先替换为真实工程的回调、任务、定时器或同步原语。
- 禁止把“高速信号必须最高驱动强度”“LED 一定可以直驱”等条件性结论写成普遍规则。驱动强度应按数据手册、负载、边沿、EMI 和实测波形选择最小可行值。
- 如果没有真实实现，写清楚“当前两套 SDK 未找到对应实现”，并给出验证入口。

## 7. 各主题的最低深度门槛

### UART

必须覆盖：异步采样、起始/数据/校验/停止位、波特率误差、FIFO（先进先出缓冲）、中断阈值和超时、DMA（直接内存访问）、共享 IO/半双工、帧错误/溢出/断线、缓冲区生命周期、真实 WQ 初始化和收发调用链、逻辑分析仪验证。

### SPI

必须覆盖：SCK/MOSI/MISO/CS 电气角色、全双工和 dummy 字节、CPOL/CPHA 四种模式、CS 时序、字节序、命令/地址/数据阶段、片选保持、半双工/双线/四线边界、频率与信号完整性、多设备冲突、真实 WQ SPI API（先从源码核实名称）、至少一个真实应用或明确记录当前项目没有 SPI 外设实例。

### GPIO

必须覆盖：输入/输出、推挽/开漏、上下拉、复用、有效电平、去抖、边沿/电平中断、ISR（中断服务程序）约束、资源占用、输出先写电平再切换方向的毛刺问题、低功耗唤醒与恢复、真实 WQ `wq_gpio_*` API、实际回调/使用点和调试方法。

### I2C

以已经完成的 `i2c-basics-I2C基础.md` 为结构样板，不要回退成摘要；新增内容必须保持“原理 → SDK → 异常 → 调试 → 练习”闭环。

### I2S/PDM/音频

必须覆盖：采样率、位宽、声道、BCLK/LRCLK 或 PDM 时钟、主从关系、时钟误差、DMA 缓冲、欠载/溢出、真实 WQ 音频 HAL/驱动/应用调用链；不能只介绍接口线名。

### PWM/定时器、ADC/DAC

必须覆盖：周期/频率、占空比、分辨率、触发与采样窗口、量化误差、校准、DMA/中断、边界和实际 SDK 配置；至少包含一个真实项目使用或明确缺口。

### MIPI/USB/SDIO

必须覆盖：分层、链路训练/枚举/初始化状态、带宽预算、缓存/队列、错误恢复、抓包或日志证据、当前 SDK 中对应框架入口；高层描述不得替代状态机和调试路径。

## 8. 文档质量检查表

每改完一个文件，逐项检查并在进度日志中记录：

- [ ] 一句话结论不是口号，而是可验证的技术结论；
- [ ] 有不超过 5 句话的“30 秒先看懂”，其中没有未解释的缩写、长代码和公式；
- [ ] 学习目标区分“第一遍能做到”和“进阶后能做到”，且均可观察、可验收；
- [ ] 术语首次出现即时解释；
- [ ] 每个小节只引入一个核心问题；连续未解释的新概念不超过 3 个；
- [ ] 费曼类比有边界，不把类比当实现；
- [ ] 至少一个完整场景演算；其时序、状态、缓冲区长度/下标、返回值和电气行为相互一致；
- [ ] 通用原理、项目事实、待确认推断明确分层；
- [ ] 至少一个真实 SDK 代码锚点，路径和符号可检索；
- [ ] 没有通用 API 冒充项目 API；
- [ ] 解释调用链、参数含义、返回值、缓冲区生命周期和回调执行上下文；同步、ISR、DMA 回调等不同上下文不混淆；
- [ ] 有正常路径、至少三类异常和可操作调试步骤；
- [ ] SDK 先给最小真实片段并逐行解释，再进入完整调用链；
- [ ] 有至少一个 1～3 分钟的小白自检、至少三个进阶练习和至少五个自测题；
- [ ] 至少链接上游基础、同层对比、下游应用中的已有页面；
- [ ] 代码围栏闭合，表格和 Obsidian 链接格式正确；
- [ ] `git diff --check` 通过；
- [ ] 修改范围只包含本批目标文件。

### 8.1 全库验收指标

最终验收不能只看字数。至少要完成：

1. 逐篇检查核心技术文档的结构和术语；
2. 扫描所有 SDK 路径，确认文件和关键符号存在；
3. 扫描 Markdown 代码围栏、表格和 Obsidian 链接；
4. 抽取每个主题至少一个“概念—代码—异常—练习”闭环；
5. 检查文档之间的学习顺序和 MOC 入口；
6. 抽样让另一个 AI 仅凭文档回答真实项目问题，再根据答错点补写；
7. 明确列出当前两套 SDK 没有覆盖的主题，不用虚构内容填空。

## 9. 推荐执行顺序

按风险和复用价值执行，每批修改前先查源码，修改后立即验收：

1. `20-HardwareProtocol-硬件协议/UART-I2C-SPI-GPIO-串口总线/spi-basics-SPI基础.md`
2. `20-HardwareProtocol-硬件协议/UART-I2C-SPI-GPIO-串口总线/uart-basics-UART基础.md`
3. `20-HardwareProtocol-硬件协议/UART-I2C-SPI-GPIO-串口总线/gpio-config-GPIO配置.md`
4. `20-HardwareProtocol-硬件协议/UART-I2C-SPI-GPIO-串口总线/uart-i2c-spi-compare-串口总线对比.md`
5. `20-HardwareProtocol-硬件协议/I2S-PDM-Audio-音频接口/i2s-protocol-I2S协议.md`
6. `20-HardwareProtocol-硬件协议/I2S-PDM-Audio-音频接口/pdm-mic-PDM麦克风.md`
7. `20-HardwareProtocol-硬件协议/I2S-PDM-Audio-音频接口/i2s-clock-tree-I2S时钟树.md`
8. `20-HardwareProtocol-硬件协议/I2S-PDM-Audio-音频接口/i2s-vs-pdm-音频接口对比.md`
9. `20-HardwareProtocol-硬件协议/PWM-Timer-定时器与PWM/pwm-basics-PWM基础.md`
10. `20-HardwareProtocol-硬件协议/ADC-DAC-Analog-模拟接口/adc-dac-ADC与DAC基础.md`
11. `20-HardwareProtocol-硬件协议/MIPI-USB-SDIO/mipi-usb-sdio-MIPI-USB-SDIO高速接口.md`
12. 更新硬件协议 MOC、根目录入口和断链。
13. 以同样方式推进 `10`—`95` 其余有效文档；优先测试、调试、RTOS、驱动、项目架构和 AI 协作主题。

如果某主题在两个真实 SDK 中没有足够代码，保留通用原理，但必须写“当前 SDK 证据缺口”和建议验证方法，不能制造项目细节。

## 10. 操作纪律

- 修改前先 `git status --short`，保护用户已有改动；
- 使用 `apply_patch` 修改本地文件；不要用 shell 重定向、Python 写文件或破坏性 git 命令；
- 每批只改目标文档和必要的交接/进度记录；
- 不修改 `/home/ys/aiglass/tina-v861/` 和两个 SDK 源码；
- 先读源码再写文档，优先使用 `rg` 定位字面路径/符号；
- 每两次检索/阅读后把关键发现写进 `findings.md` 或 `progress.md`；
- 不要因为文件很长就堆砌重复内容；深度来自约束、调用链、故障和练习；
- 不要把“真实 SDK 代码”理解成复制整文件，关键是可追溯的片段和正确解释。

## 11. 交付报告格式

每批完成后在 `progress.md` 记录：

```markdown
### YYYY-MM-DD：主题名
- 修改文件：...
- 新增深度：原理/约束/SDK/异常/调试/练习分别是什么
- SDK 证据：路径 + 符号
- 验证：git diff --check、路径检索、链接/围栏检查结果
- 遗留缺口：...
```

最终交付时必须说明：

- 哪些文件已重构；
- 哪些 SDK 事实已验证；
- 哪些主题仍只有通用原理或存在证据缺口；
- 哪些检查已通过；
- 还有哪些内容不应被误认为“已经学懂学透”。

## 12. 接手后的第一步

不要先批量改写。先执行：

```bash
cd "/home/ys/wq7036a/obsidian/嵌入式知识库2.0"
git status --short
sed -n '1,260p' "嵌入式知识库2.0-全库重构交接规范.md"
sed -n '1,240p' task_plan.md
sed -n '1,240p' findings.md
sed -n '1,240p' progress.md
sed -n '1,220p' "AI时代的软件手艺人.md"
```

然后读取 I2C 深度样板，核实真实 SDK 文件后再开始 SPI。每次只改一批，完成后先自验收，再继续下一批。
